int main (int argc,char *argv[]) {
	int a,b,c; 
	sscanf(argv[1],"%d",&a); // test case
	sscanf(argv[2],"%d",&b); // random seed
	sscanf(argv[3],"%d",&c); // also random seed
	srand(b);
	// write stuff here
}